[
  {
    "topic": "channels",
    "insight": "ChatControl's Discord integration requires the DiscordSRV plugin. Users must disable DiscordSRV's built-in chat relay by setting `DiscordChatChannelMinecraftToDiscord: false` in DiscordSRV's `config.yml`, otherwise messages get duplicated. ChatControl checks this on startup and logs a warning if it's still enabled (see ChatControl.java line ~247).",
    "related_skill": "channels",
    "scope": "project",
    "date": "2026-02-15",
    "issue": 3465
  },
  {
    "topic": "chat-formatting",
    "insight": "MiniMessage interactive tags (click, hover) in player messages require both: (1) the context listed in `Colors > Apply_On` in settings.yml (e.g. `private_message`, `chat`) and (2) the `chatcontrol.action.{action_name}` permission. Default `Apply_On` is empty, blocking all MiniMessage tags. Users reporting click/hover abuse should check these two settings.",
    "related_skill": "chat-formatting",
    "scope": "project",
    "date": "2026-03-15",
    "issue": 3509
  }
]