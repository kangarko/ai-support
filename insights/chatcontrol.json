[
  {
    "topic": "channels",
    "insight": "ChatControl's Discord integration requires the DiscordSRV plugin. Users must disable DiscordSRV's built-in chat relay by setting `DiscordChatChannelMinecraftToDiscord: false` in DiscordSRV's `config.yml`, otherwise messages get duplicated. ChatControl checks this on startup and logs a warning if it's still enabled (see ChatControl.java line ~247).",
    "related_skill": "channels",
    "scope": "project",
    "date": "2026-02-15",
    "issue": 3465
  },
  {
    "topic": "channels",
    "insight": "Channel modes are only WRITE (send+receive) and READ (receive-only) \u2014 no \"forward outgoing but hide incoming proxy messages\" option exists. The workaround for optional cross-server chat is two channels with the same format (`global` with `Proxy: true`, `local` with `Proxy: false`), but the trade-off is that local-mode players are local in both directions.",
    "related_skill": "channels",
    "scope": "project",
    "date": "2026-03-09",
    "issue": 3504
  }
]